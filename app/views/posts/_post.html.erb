<% if post.id != 1 %>
  <hr class="middle-hr">
<% end %>

<div class="feed-post">
  <img class="left-image" src="<%= post.image_url %>"
  <p>Hi, I am feed post # <%= post.id %>. The date and time is <%= post.created_at %>. <% post.body %></p>
</div>
